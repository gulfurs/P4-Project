using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Metronome : MonoBehaviour
{
    /*private bool hasStarted = false;

    void Update()
    {
        if (!hasStarted)
        {
            StartCoroutine(HihatCoroutine());
        }
    }

    public IEnumerator HihatCoroutine()
    {
        hasStarted = true;
        DrumManager.PlaySound(DrumManager.DrumSound.Hihat);
        yield return new WaitForSeconds(1f);
        hasStarted = false;
    }
    */
}
